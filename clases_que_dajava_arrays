ARRAYS CLASES DE JAVA

  - binary search (int[], int key) --> busca key en el array --> el indice del array donde está ese elemento
  
  - copyofRange (int[] original, int from, int to) --> Devuelve un numero array que es un trozo del original. Lo corta desde el indice "from" hasta "to"
  
  - equals (int[] a, int[] b) --> comparar si los arrays son iguales, si sus elementos coinciden
  
  - fill (int[] a, int value) --> rellena un array con VALUE en todos sus elementos
  
  	*ARRAYS SE PASAN A LAS FUNCIONES POR REFERENCIA
  	
  - sort (int[] a) --> Ordena numericamente los valores del array
  
  - toString (int[]) --> convertir un valor a un un String
