import java.util.Arrays;
public class ArraysYMatrices19 {

    public static boolean comprobarLlena(int vector[], int posición){
        return (posición == vector.length -1); //Si el elemento es igual a la última posición del vector, devuelve verdadero
    }
    public static boolean comprobarVacia(int posición){
        return (posición == -1); //Si el elemento es igual "-1" (posición vacía), devuelve verdadero
    }

    public static void main(String[] args) {
        
        int pila[] = new int[20];
        int posición = -1; //Nos indica que la pila está vacía
    }
    
}
